<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Реквизиты продавца по умолчанию
    |--------------------------------------------------------------------------
    |
    | Эти данные будут подставляться в УПД, если при формировании документа
    | не указать свои реквизиты.
    | Можно задавать через переменные окружения .env.
    |
    */

    'seller' => [
        'name'    => env('UPD_SELLER_NAME', 'АВТОиностранец'),
        'inn'     => env('UPD_SELLER_INN', ''),
        'kpp'     => env('UPD_SELLER_KPP', ''),
        'address' => env('UPD_SELLER_ADDRESS', ''),
        'phone'   => env('UPD_SELLER_PHONE', ''),
    ],


    /*
    |--------------------------------------------------------------------------
    | Ставка НДС по умолчанию
    |--------------------------------------------------------------------------
    |
    | Если в товарной позиции не указана ставка НДС (vat_rate), то будет
    | применяться эта ставка. По умолчанию 20.
    |
    */

    'default_vat_rate' => 20.0,


    /*
    |--------------------------------------------------------------------------
    | Тип цены по умолчанию
    |--------------------------------------------------------------------------
    |
    | Определяет, как интерпретировать поле "price" в товарной позиции, если
    | у товара не указано "price_type".
    |
    | Возможные значения:
    |
    |   'net'   — цена указана без НДС;
    |   'gross' — цена указана с НДС.
    |
    | Пример:
    |   price = 100, vat_rate = 20
    |
    |   'net'   → net = 100, vat = 20, gross = 120
    |   'gross' → gross = 100, vat = 16.67, net = 83.33
    |
    */

    'default_price_type' => 'net',


    /*
    |--------------------------------------------------------------------------
    | Шаблон HTML для генерации УПД
    |--------------------------------------------------------------------------
    |
    | Laravel view, который используется DomPDF для генерации PDF.
    | У тебя по умолчанию используется:
    |
    |   resources/views/vendor/laravel-upd/upd.blade.php
    |
    | Ты можешь переопределить его, создав свой blade-файл и указав здесь путь.
    |
    */

    'view' => 'laravel-upd::upd',
];
